# Nicos-Super-Mods-Trust-Registry
NSM Trust Registry contains verified SHA-512 hashes for official Minecraft mod releases. The registry is cryptographically signed and consumed by NSM ModGuard to detect modified, impersonated, or unknown mod JARs.

A listed hash confirms that a file exactly matches a known release. It does not guarantee that the release itself is safe or free from vulnerabilities.
